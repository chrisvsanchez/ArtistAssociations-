 <%= form_for @song do |f| %>
<%= f.text_field :title %>
<%= f.text_field :genre %>
<%= f.label :artist_id %>
<%= collection_select(:song, :artist_id, Artist.all,:id, :name )%>

<%= f.submit %>
<%end%>
